<%- include("partials/header") %>

<section class="pricing-section">
  <h2>Our Pricing</h2>

  <div class="pricing-grid">
    <% services.forEach(s => { %>
      <div class="price-card">
        <h3><%= s.name %></h3>
        <div class="price">₹<%= s.price %></div>
        <a href="/order?service=<%= s.name %>&amount=<%= s.price %>" class="btn-primary">
          Order Now
        </a>
      </div>
    <% }) %>
  </div>

  <p style="margin-top:20px;font-size:14px;text-align:center">
    All service prices are clearly displayed. Final scope and deliverables are
    confirmed with the customer before work begins. No hidden charges apply.
  </p>
</section>

<%- include("partials/footer") %>